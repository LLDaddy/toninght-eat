import 'package:flutter/material.dart';

class FailFixturePage extends StatelessWidget {
  const FailFixturePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('测试硬编码文案'),
      ),
    );
  }
}

